version: "3.7"
networks:
  default:
    external: true
    name: retrace_network
services:
  web:
    container_name: java_bad_app
    build: .
    restart: on-failure
    volumes:
      - /usr/local/stackify:/usr/local/stackify
    ports:
      - "8081:8081"
